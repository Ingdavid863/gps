<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Keep AR optional so the normal MapLibre navigation screen remains usable
         on phones that do not support Google Play Services for AR. -->
    <uses-feature
        android:name="android.hardware.camera.ar"
        android:required="false" />

    <application
        android:allowBackup="true"
        android:hardwareAccelerated="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher"
        android:label="GPS3D AR David"
        android:theme="@style/Theme.GPS3DAR"
        android:usesCleartextTraffic="false">

        <!-- SceneView/ARCore can install/update Play Services for AR when needed. -->
        <meta-data
            android:name="com.google.ar.core"
            android:value="optional" />

        <!-- Injected from ARCORE_API_KEY in GitHub Actions or local.properties.
             Empty keeps camera AR working, but Geospatial/Streetscape disabled. -->
        <meta-data
            android:name="com.google.android.ar.API_KEY"
            android:value="${arcoreApiKey}" />

        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <activity
            android:name=".RealisticMapActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- Native AR/Filament view is now the main entry point. -->
        <activity
            android:name=".ArNavigationActivity"
            android:exported="true"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
